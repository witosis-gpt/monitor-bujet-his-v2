name: Recovery and regression checks
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Syntax checks
        run: |
          node --check supabase-client.js
          node --check cloud-save-fix.js
          node --check module4-data.js
          node --check app.js
          node --check module4.js
      - name: Cloud recovery tests
        run: node scripts/test-cloud-recovery.js
      - name: Planner cloud tests
        run: node scripts/test-module4-cloud.js
      - name: Module 4 account regression tests
        run: node scripts/test-module4-524113.js
      - name: Travel regression tests
        run: npm run test:travel
      - name: Module 4 Excel export tests
        run: node scripts/test-module4-excel-export.js
